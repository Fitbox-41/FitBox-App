<?xml version="1.0" encoding="utf-8"?>
<!-- The window the OS shows while the process starts, in dark mode. Without
     this the default white plate flashes before the app's near-black first
     frame — very visible now that the launcher icon is on the same black. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/ic_launcher_background" />
</layer-list>
